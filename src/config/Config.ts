const DEVAPI = process.env.DEVAPI;
const PRODAPI = process.env.PRODAPI;
const PROD = process.env.PROD;
const DEV = process.env.DEV;

export const Core = {
    DEVAPI,
    PRODAPI,
    DEV,
    PROD,
}